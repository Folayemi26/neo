// app/utils/Compression.js

export async function compressString(str) {
  // TODO: Replace with real compression (pako / zlib) if needed
  return str;
}

export async function decompressString(str) {
  // TODO: Replace with real decompression
  return str;
}

